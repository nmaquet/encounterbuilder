'use strict';

DEMONSQUID.encounterBuilderServices.factory('lootService', [
    function () {
        var service = {};

        service.randomizeLoot = function(encounter) {
                    
        };

        return service;
    }]);
