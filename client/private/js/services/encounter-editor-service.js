'use strict';

DEMONSQUID.encounterBuilderServices.factory('encounterEditorService', [
    function () {

        var service = {
            encounter : null
        };

        return service;
    }]);